# Agentic World -- Fine-Tuning Dependencies
# Install: pip install -r requirements.txt

# Core ML
torch
torchvision
torchaudio

# Model loading + quantization
transformers>=4.45.0
bitsandbytes>=0.43.0

# LoRA adapters
peft>=0.12.0

# Supervised fine-tuning
trl>=0.9.0

# Utilities
accelerate>=0.33.0
datasets
huggingface-hub

# Flash Attention 2 (optional but recommended for speed)
# pip install flash-attn --no-build-isolation

# Experiment tracking (observation only)
wandb